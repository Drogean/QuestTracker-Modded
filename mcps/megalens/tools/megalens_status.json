{
  "name": "megalens_status",
  "description": "Poll MegaLens: without run_id, returns server config + plan tier. With run_id, returns progress for that async audit run (checklist, host_guidance, terminal payload when approved).",
  "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "run_id": {
        "description": "Optional MCP async run UUID. When provided, returns poll state instead of config info.",
        "type": "string",
        "format": "uuid",
        "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$"
      }
    }
  }
}