---
description: ONE agent builds and ships. No QT coordinator/auditor/handoff rules.
alwaysApply: true
---

# Single agent mode

You **audit, code, and ship** in one chat. No separate Auditor or Builder.

## Deleted / gone — do not resurrect

Quest Tracker coordinator rules were removed: BUILDER HANDOFF, AUDIT DEBT, auditor-no-edits, thermo-nuclear audit, MegaLens mandatory passes, builder handoff gates.

## Pawn Companion (default for this user)

- Path: `OTHERMODS/PawnCompanion/`
- Rule: `PawnCompanion/.cursor/rules/pawn-companion-workflow.mdc`
- **Not Quest Tracker.** Do not suggest QT bridges or `quest_tracker_log` for PC work.

## Ship (short)

1. Read `re2_framework_log.txt`
2. Fix minimal diff
3. Bump version + zip
4. Tell user: install, repro, what log line proves PASS

## PC hard limits

- `questmanager_a` / `questmanager_b` = 200 locals — new logic in new files only
- Ship proof: `loaded (N modules)`, zero `MODULE FAIL`
