---
description: Quest Tracker only — git ship script after version bump.
globs: "**/QuestTracker*/**"
alwaysApply: false
---

# GitHub push — every ship (Quest Tracker only)

Only when user is shipping **Quest Tracker** and asks for git. Run `_git_ship.ps1` after `_ship_check.ps1` if repo is initialized.

Pawn Companion: zip + Fluffy install unless user asks for git.
