# Version checkpoint (backtrack)

Before shipping or big map/pin changes, read `VERSION_CHECKPOINT.md` in repo root.

**Current safe pin:** `1.3.2` (2026-06-10). v1.3.3 was handoff-only, never built.

If user says "backtrack" or quality regressed: stop, confirm log shows `mod loaded v1.3.2`, do not layer new fixes on unshipped handoffs.
